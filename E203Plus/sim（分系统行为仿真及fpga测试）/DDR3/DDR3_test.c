// See LICENSE for license details.
#include <stdio.h>
#include <unistd.h>
#include "platform.h"

uint8_t message[72]={
		0xff,0xff,0xff,0xff,0xff,0xff,0x00,0x00,0x00,0xec,0xaf,0xec,
		0x00,0x3a,0xff,0xff,0x22,0x33,0x44,0x55,0x66,0x77,0x88,0x99,
		0x00,0x11,0x22,0x33,0x44,0x55,0x66,0x77,0x88,0x99,0x99,0x99,
		0x00,0x11,0x22,0x33,0x44,0x55,0x66,0x77,0x88,0x99,0x99,0x99,
		0x00,0x11,0x22,0x33,0x44,0x55,0x66,0x77,0x88,0x99,0x99,0x99,
		0x00,0x11,0x22,0x33,0x44,0x55,0x66,0x77,0x88,0x99,0x99,0x99
};
#define GIE 0x100507f8
#define TX_PING_LEN 0x100507f4
#define TX_PING_CON 0x100507fc
#define TX_BASE 0x10050000;
#define RX_BASE 0x10051000;
#define RX_PING_CON 0x100517fc
#define MDIO_ADDR 0x100507e4
#define MDIO_WR 0x100507e8
#define MDIO_RD 0x100507ec
#define MDIO_CTRL 0x100507f0
int main(void)
{
	int i;
	volatile uint32_t *ptr_w;
	volatile uint16_t *ptr_h;
	volatile uint8_t * ptr_b;
	ptr_w = (uint32_t *)0xa0000000;
	ptr_h = (uint16_t *)0xa0000000;
	ptr_b = (uint8_t *)0xa0000000;
	for (i=0;i<100;i++){
		*(ptr_w+i)=i;
	}
	for (i=0;i<100;i++){
		write_hex(STDERR_FILENO, *(ptr_w+i));
		write(STDERR_FILENO, " ", 1);
	}
	write(STDERR_FILENO, "\n", 1);

	for (i=0;i<100;i++){
		*(ptr_h+i)=i;
	}
	for (i=0;i<100;i++){
		write_hex(STDERR_FILENO, *(ptr_h+i));
		write(STDERR_FILENO, " ", 1);
	}
	write(STDERR_FILENO, "\n", 1);

	for (i=0;i<100;i++){
		*(ptr_b+i)=i;
	}
	for (i=0;i<100;i++){
		write_hex(STDERR_FILENO, *(ptr_b+i));
		write(STDERR_FILENO, " ", 1);
	}
	write(STDERR_FILENO, "\n", 1);

	return 0;
}

